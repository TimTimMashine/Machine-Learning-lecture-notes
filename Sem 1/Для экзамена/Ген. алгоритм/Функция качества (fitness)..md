Функция качества (или целевая функция) в генетическом алгоритме — это мера того, насколько хорошо особь (решение) подходит для решения задачи. Она оценивает степень приспособленности кандидатов в популяции, и на основе этого происходит отбор особей для дальнейшего размножения.

Основные моменты:

1. **Целевая функция**: она задает критерий оптимизации задачи, то есть функция, которую генетический алгоритм пытается минимизировать или максимизировать. В задачах оптимизации это обычно оценка того, насколько результат близок к оптимальному.
    
2. **Применение в генетическом алгоритме**:
    
    - Каждая особь в популяции оценивается функцией качества.
    - Чем выше значение функции качества (или чем ближе к цели задачи), тем выше вероятность, что эта особь будет выбрана для скрещивания и мутации.
    - Часто эта функция зависит от генов особи (параметров решения задачи) и может включать как жесткие критерии (например, выполнение ограничений), так и мягкие (например, минимизация ошибок).

Пример: Если решаем задачу минимизации функции f(x), то функция качества может быть просто значением f(x), а алгоритм будет стремиться минимизировать её. В случае задачи максимизации — наоборот, алгоритм будет стремиться найти максимальное значение функции.

Как ты видишь, для разных задач функция качества может быть разной по структуре и сложности.

# Случай однопараметрической оптимизации
Фитнесс-функция возвращает **число**

# Случай многопараметрической оптимизации

Фитнесс-функция возвращает **вектор целевых функций, которые нам нужно оптимизировать.**

## Связь Парето-доминирования и многопараметрической оптимизации

В многокритериальных эволюционных алгоритмах (MOEA) Парето-доминирование используется для **ранжирования популяции**. Самый известный подход — **недоминируемая сортировка** (non‑dominated sorting).

**Этапы работы ГА с Парето-доминированием:**

1. **Формирование фронтов Парето**  
    Вся популяция делится на слои (фронты):
    
    - **Первый фронт** — все недоминируемые решения текущей популяции (оптимальные по Парето внутри выборки).
        
    - **Второй фронт** — решения, которые становятся недоминируемыми после удаления первого фронта.
        
    - И так далее.
        
2. **Присвоение ранга приспособленности**  
    Каждому решению присваивается ранг, равный номеру его фронта. Чем меньше ранг, тем особь ближе к истинной границе Парето и тем выше её шансы быть выбранной для скрещивания.
    
3. **Сохранение разнообразия (crowding distance)**  
    Если отбирать только по рангу, алгоритм быстро сойдётся к одному участку границы Парето. Чтобы покрыть весь спектр компромиссов, вводят **расстояние скученности**. Для решений одного фронта предпочтение отдаётся тем, у кого меньше соседей (больше «свободного места» вокруг). Это позволяет равномерно распределить популяцию вдоль всего Парето-фронта.

### Расстояние скученности

#### Зачем
![[Pasted image 20260212223840.png]]
#### Как вычисляется
![[Pasted image 20260212223805.png]]