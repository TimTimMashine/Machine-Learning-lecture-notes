### С использованием момента

Алгоритм **градиентного спуска с моментом (Gradient Descent with Momentum, GDM)** является расширением стандартного градиентного спуска, в котором учитываются предыдущие шаги для ускорения сходимости и уменьшения колебаний в процессе оптимизации. 

![[Pasted image 20241110143013.png]]
#### **Градиентный спуск с моментом**:

$$ 
\displaylines{

\text{grad} \, f(\vec{x}) = \left\{ \frac{\partial f}{\partial x_i} \right\}_{i=1}^n \\


\vec{x} = \{x_1, x_2, x_3, x_4, \dots\} \\


\vec{m} = 0, \quad \vec{x} = 0, \\
 \quad \alpha \in [0,1], \quad \alpha \rightarrow 1 \\

\text{for } i = 1 \text{ to } n:
\quad \\

 ~ g = \text{grad} \, (f(x)) \\  ~~~~~~~~~~~~~~m = \alpha \cdot m + (1 - \alpha) \cdot g \\ x = x - m ~~~~~~~~



}


$$

![[Снимок экрана 2025-04-13 220449 1.png]]
Градиентный спуск с моментом использует дополнительную переменную — **скорость** скорость 𝑣𝑡 (коэффициент моментума * на скорость) которая учитывает не только текущий градиент, но и информацию о том, как изменялись градиенты на предыдущих шагах. Это позволяет сделать шаги более направленными и сгладить колебания.

##### Проблема момента
**Медленно останавливается**: пролетаем точку минимума и качаемся перед тем, как остановиться

Обновление с моментом выполняется по следующим формулам:

![[Pasted image 20241110143209.png]]

![[Pasted image 20241110143237.png]]

![[Pasted image 20241110143407.png]]

**Результат**: Когда условие остановки выполнено, возвращаете финальное значение x, которое является приблизительным минимумом функции.

##### Почему использование (моментума???) ускоряет оптимизацию?

1. **Ускорение сходимости**:
    
    - Моментум позволяет ускорить сходимость, так как скорость обновлений на каждом шаге учитывает не только текущий градиент, но и накопленную информацию о предыдущих шагах. Это помогает быстрее двигаться в правильном направлении, особенно если функция имеет "гладкие" спуски
    
2. **Сглаживание колебаний**:
    
    - Если градиент функции меняется очень резко на разных шагах (например, в случае сильно изогнутых или неровных функций), использование моментума помогает сгладить колебания, так как предыдущие шаги увеличивают вклад в направлении обновления, несмотря на местные изменения градиента.

![[Pasted image 20241110143635.png]]

![[Pasted image 20241110143607.png]]

-----------------
![[Pasted image 20241127005720.png]]


![[Pasted image 20250204221655.png]]
показывает сколько примерно приближенно последних градиентом мы будем усреднять и их учитывать, у осттальных будет маленькое значение коэф перед ними


-----------

