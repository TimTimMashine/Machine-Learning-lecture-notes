# Идея
Точка является корневой, если в её эпсилон окрестности находится как минимум n точек (включая её саму). 

Более простая реализация считает и саму точку.

# Алгоритм
для каждой точки 
  Если $label(p)$ не принадлежит ни к одному кластеру и она корневая

# Параметры
$\varepsilon$ - 
$n$ -

# Проблемы
1. Зависит от порядка обхода точек (для точек, которые находятся на границах кластеров)
2. Плохо работает с переменной плотностью