#### **1. Что такое логистическая регрессия?**

Логистическая регрессия — это метод машинного обучения, который используется для бинарной классификации. Она помогает предсказать вероятность принадлежности объекта к одному из двух классов. Например:

- Будет ли клиент покупать продукт? (1 — да, 0 — нет)
- Является ли письмо спамом? (1 — спам, 0 — не спам)
- Пройдет ли студент экзамен? (1 — да, 0 — нет)

В отличие от линейной регрессии, которая предсказывает непрерывные значения (например, цену квартиры), логистическая регрессия предсказывает вероятность события.

 

#### **2. Математическая модель**

Основная идея логистической регрессии — это использование **логистической функции (сигмоиды)** для преобразования линейной комбинации признаков в вероятность.

**Общая формула модели:**
$$\displaylines{

f:X \rightarrow \{0,1\} \\
log({\dfrac{p}{1-p}}) = \theta \cdot x, 
\text{ где } p \text{ - это вероятность того, что объект принадлежит этому классу 1, а  }\\ 1 - p \text{ - вероятность того, что не принадлежит этому классу}
}$$
$z=β_0​+β_1​x_1​+β_2​x_2​+⋯+β_nx_n$

Распределение бинарное (Бернулли), события взаимоисключающие, поэтому одно событие имеет вероятность $p$, а другое $1 - p$

$$p = f(x, \theta)$$

Хотим выразить $p$ из $log({\dfrac{p}{1-p}}) = \theta \cdot x$  для этого потенцируем, получим:
$$\displaylines{\dfrac{p}{1-p} = e^{\theta \cdot x}  \\
}$$
 Домножим на  $1-p$ получим:
 $$\displaylines{ 
 p = e^{\theta \cdot x} \cdot (1-p) \\
 p = e^{\theta \cdot x} - e^{\theta \cdot x} \cdot p \\
 p +  e^{\theta \cdot x} \cdot p = e^{\theta \cdot x}  \\
 p \cdot (1 + e^{\theta \cdot x} ) =  e^{\theta \cdot x} \\
 p = \dfrac{e^{\theta \cdot x}}{1 + e^{\theta \cdot x} } =  \dfrac{1}{\dfrac{1}{e^{\theta \cdot x}} + 1} = \dfrac{1}{ 1 + e^{-\theta \cdot x}} \\

 }
 
$$

Получили, что 
$$p = f(x, \theta) = \sigma (\theta \cdot x)$$
##### Почему используем именно отношение вероятностей
Есть функция, которая умеет предсказывать какие-то числа (от $-\infty$ до $+\infty$), а мы хотим выдать этому числу какой-то смысл, чтобы не накладывать никаких ограничений на $\theta \cdot x$. С обычной вероятностью так сделать нельзя, потому что она ограничена 1. Отношение тоже не получится (?), поэтому решили прологорифмировать.

Если не брать логарифм, то нет волшебного распределения (без логарифма получается плохо), числа не удовлетворяют требованиям вероятностей.

Смысл отношения: во сколько раз вероятность того, что принадлежим какому-то классу больше чем то, что ему не принадлежим.  


![[Pasted image 20250205220344.png]]

![[Pasted image 20250205220402.png]]

![[Pasted image 20250205220437.png]]
##### Смысл параметров
$$\displaylines{ \log \dfrac{p}{1+b} = a\cdot x + b \text{ линейный случай}\\
p = \dfrac{1}{1 + e^{a\cdot x + b}}\\

\dfrac{p}{1-p} = e^{ax + b} \\

\dfrac{e^{ax+b}}{e^{a(x+1)+b}} = \dfrac{e^{ax} \cdot e^{b} } {e^{ax} \cdot e^{a} \cdot e^{b}}= \dfrac{1}{e^a}
}$$

Смысл в следующем: там где добавили единицу, шанс того, что это класс 1 увеличился во столько раз ( $e^a$ ). 

![[Pasted image 20250205220830.png]]
###### Откуда выводится формула бинарной кросс-энтропии?
Из метода [[Метод наибольшего правдоподобия.|метода максимального правдоподобия]]
##### Лосс для логистической регрессии
$$  \displaylines{
L =\begin{cases} -log ~ p,~ y =1 \\
 -log ~ (1-p),~ y = 0
 \\
\end{cases}

}$$Обычно мы хотим максимизировать, а мы хотим минимизировать, поэтому пишем минус. 

Но формуле функции потерь написан if мы хотим избавиться от if, перепишем в другом виде: 
$$\displaylines{ L = - y ~ log(p) - (1-y) ~log(1-p) \text{ - Бинарная кроссэнтропия} \\
y_{1}~~~~~~~  p_{1}~~~~~~~~~y_{2}~~~~~~~~~ p_2 
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~  \\\text{Если }y = 1, \text{то останестя только первое слагаемое, а если ноль то только второе.} \\
 \text{И  этот лосс хотим миниммизировать.} \\
\text{Итого получим в общем виде получим кроссэнтропию: }\\ \\
-\sum\limits{y_{i} ~ log ~ p_i}
}$$ 
##### В каких пределах лежат значения лосса? 

$$\displaylines{ 0 \leq p \leq 1 \Leftrightarrow -\infty \leq log ~ (p) \leq 0 \Leftrightarrow 0 \leqslant - log ~ p \leqslant \infty}$$ 

В numpy нужно использовать функцию xlogy вместо обычного логарифма, она вернёт ноль, если x равен нулю. А если написать x * log y, то получим либо Nan либо бесконечность (в зависимомти от того, что вернёт библиотека)

##### Как минимизировать Лосс
Найдём производную, приравняем к нулю(это формула для случая, когда y равен либо ноль, либо 1)
$$\displaylines{ -y ~ log ~ (\sigma(\theta \cdot x)) - (1-y) ~ log(1 -\sigma(\theta \cdot x)) \tag{1}\\ }$$

Можем получить логарифим 1 (ноль не страшно) и логарифм нуля (минус бесконечность страшно)
$$
\displaylines{
log(\sigma (\theta \cdot x))^{'}_{\theta} = \dfrac{1}{\sigma(\theta 

\cdot x)} \cdot (\sigma(\theta \cdot x)) ^{'}_{\theta}  \\

(\sigma(\theta \cdot x)) ^{'}_{\theta} = \left(\dfrac{1}{1+e^{-\theta x}}\right)^{'}_{\theta} = - \dfrac{1}{(1+e^{-\theta x})^{2}}\cdot(1+e^{-\theta \cdot x})^{'}_{\theta} = \dfrac{e^{-\theta \cdot x} \cdot x}{(1+e^{-\theta \cdot x})^{2}} \\= \sigma^{2}(\theta \cdot x)\cdot e^{-\theta \cdot x} \cdot x \\\\

log(\sigma (\theta \cdot x))^{'}_{\theta} = \dfrac{1}{\sigma(\theta 

\cdot x)} \cdot (\sigma(\theta \cdot x)) ^{'}_{\theta} =  \dfrac{\sigma^{2}(\theta \cdot x)\cdot e^{-\theta \cdot x} \cdot x}{\sigma(\theta \cdot x)} =  \sigma (\theta \cdot x)\cdot e^{-\theta \cdot x} \cdot x

}$$

Другой вариант
$$\displaylines{log(\sigma (\theta \cdot x))^{'}_{\theta} = log(1) - log(1+e^{-\theta \cdot x})^{'}_{\theta} = \dfrac{-1}{ 1 + e^{-\theta \cdot x}} \cdot (1 + e^{-\theta \cdot x})^{'}_{\theta} = \\ = -\sigma(\theta \cdot x) \cdot e^{-\theta \cdot x} \cdot (-\theta \cdot x)^{'}_{\theta} = \sigma(\theta \cdot x) \cdot e^{-\theta \cdot x} \cdot x

}$$

Теперь вернёмся к формуле $(1)$, найдём производную второй части $$\displaylines{1 - \sigma(\theta x) = \dfrac{1+e^{-\theta x}}{1+e^{-\theta x }} - \dfrac{1}{1+e^{-\theta x }} = \dfrac{e^{-\theta x }}{1+e^{-\theta x }} \\

\text{Поделим числитель и знаменетель на } e^{-\theta x} \text{ получим:}\\

\dfrac{e^{-\theta x }}{1+e^{-\theta x }} = \dfrac{1}{\dfrac{1}{e^{-\theta x}} + \dfrac{e^{-\theta x}}{e^{-\theta x}}} = \dfrac{1}{1 + e^{\theta x} } = \sigma(-\theta x)

}$$
$$\displaylines{log(\dfrac{1}{1+e^{\theta x}})^{'}_{\theta} = log(1)^{'}_{\theta} - log(1+e^{\theta \cdot x})^{'}_{\theta} = 
\dfrac{-1}{ 1 + e^{\theta \cdot x}} \cdot (1 + e^{\theta \cdot x})^{'}_{\theta} = \\ = -\sigma(-\theta \cdot x) \cdot e^{\theta \cdot x} \cdot (\theta \cdot x)^{'}_{\theta} = -\sigma(-\theta \cdot x) \cdot e^{\theta \cdot x} \cdot x
}$$

Тогда производная по $\theta$ от формулы $(1)$ будет иметь следующий вид:

$$\displaylines{
-y ~ \sigma(\theta \cdot x) e^{-\theta x} \cdot x + (1-y) ~ \sigma(-\theta \cdot x)) e^{\theta x} \cdot x \tag{2}\\

}$$
##### Случай, когда y=1 и y=-1
Нам нужна функция следующего вида:
$$\displaylines{\tilde{y} = f(y) = \begin{cases} y = 1, ~\tilde{y} = 1 \\
y = 0, ~\tilde{y} = -1 \\
\end{cases}\\\\

\tilde{y} = 2y - 1
}$$

Теперь перепишем формулу $(2)$ в следующем виде:

$$\displaylines{
-\sigma(\tilde{y}\cdot\theta \cdot x ) e^{-\tilde{y} \cdot\theta \cdot x} \cdot ~\tilde{y} \cdot x\\\\

\text{сделав переобозначение } \tilde{x} = \tilde{y} \cdot x \text{ получим: }\\ 

-\sigma(\theta \cdot \tilde{x} ) e^{-\theta \cdot \tilde{x}} \cdot ~\tilde{x}\\\\

\text{теперь избавимся ещё и от экспоненты, поделив на } 1 + e^{-\theta \cdot x} \text{ получим:}\\

-\sigma(-\theta \cdot \tilde{x} ) \cdot ~\tilde{x}\\\\
}$$
$$$$
#### Рассмотрим задачу классификации на несколько классов

$$\displaylines{f~|~x \rightarrow C - \text{ где классов несколько }|C| = K \\\\

}$$
##### Выберем один класс и противопоставим его всем остальным

$$\displaylines{
\text{Выберем один класс и противопоставим его всем остальным}\\

log\left(\dfrac{p_i}{p_{k}}\right) = \theta_{i}\cdot x \\

p_{i}=P(y=C_{i})~-~\text{вероятность, что $y$ из класса $i$ },~ i = \overline{1, k-1}\\
p_{k}=P(y=C_{k}) ~-~\text{вероятность, что $y$ из класса $k$ } \\\\
(\text{И таких классов }k-1), \text{где } \theta_{i} - \text{веса для $i$-того класса, т.е. теперь строим ни одну } \\ \text{ логистическую (в видео было линейную) регрессию,  а много,  где мы сравниваем } \\ \text{ вероятность каждого класса с вероятностью класса k } \\\\

}$$
$$\displaylines{
\text{Потенцируем:}\\

\dfrac{p_i}{p_{k}} = e^{\theta_{i}~\cdot~ x} \\
p_{k} =\dfrac{p_i}{e^{\theta_{i}~\cdot~ x}} \\
p_{i} = p_{k} \cdot e^{\theta_{i}~\cdot~ x} \tag{3}\\\\

}$$
$$ \displaylines{
\text{Но ещё мы знаем, что }\\\\

p_{k}= 1 - \sum\limits_{i=1}^{k-1}p_{i}= 1 - p_{k} \cdot \sum\limits_{i=1}^{k-1} e^{\theta_{i}~\cdot~ x} \\\\

p_{k} + p_{k} \cdot \sum\limits_{i=1}^{k-1} e^{\theta_{i}~\cdot~ x} = 1 \\

p_{k} \cdot \left(1 + \sum\limits_{i=1}^{k-1} e^{\theta_{i}~\cdot~ x} \right) = 1 \\\\

p_{k}  = \dfrac{1}{\left(1 + \sum\limits_{i=1}^{k-1} e^{\theta_{i}~\cdot~ x} \right)} \\\\

\text{А теперь вернёмся к формуле $(3)$ выризим $p_i$ через $p_k$:}\\

p_{i} = \dfrac{1}{\left(1 + \sum\limits_{i=1}^{k-1} e^{\theta_{i}~\cdot~ x} \right)} \cdot e^{\theta_{i}~\cdot~ x} =  \dfrac{e^{\theta_{i}~\cdot~ x}}{\left(1 + \sum\limits_{i=1}^{k-1} e^{\theta_{i}~\cdot~ x} \right)}
}$$

$$ \displaylines{\text{Взяли конкретный $x$ посчитали вероятности всех классов от 1 до $k-1$ через $p_i$, } \\\text{ посчитали вероятность класса $k$,теперь нужно выбрать среди них максимум} }$$

Заводим k-1 линейную регрессию считаем по ним вероятности, получаем ответ.

В качестве функции лосса используем кросс-энтропию

$$\displaylines{
L = -\sum\limits_{i=1}^{k} y_{i} \cdot log (p_{i}) \tag{4} 
}$$
$y$-ки все будут нулями кроме одного

$$\dfrac{a}{b}$$
##### Второй вариант многоклассовой логистической регрессии
$$\displaylines{ log(p_{i)}=\theta_{i}\cdot x\\

\log\left(\dfrac{p_{i}}{z}\right) = \theta_{i}\cdot x\\

log(p_{i}) = \theta_{i} \cdot x + log(z)\\
p_{i}= e^{\theta_{i} ~ \cdot ~ x ~+~ log(z)} = z \cdot e^{\theta_{i} ~ \cdot ~ x}\\\\

\text{Но ещё } ~~ \sum\limits p_{i}= 1 \leftrightarrow z\cdot \sum\limits e^{\theta_{i} ~ \cdot ~ x} = 1 \ \leftrightarrow z = \dfrac{1}{\sum\limits e^{\theta_{i} ~ \cdot ~ x}}

}$$

Можем переписать всё тоже самое, но не поделив на нормировочный коэффициент, а домножив на него

$$\displaylines{ log(p_{i})=\theta_{i}\cdot x\\

\log\left(p_{i}{z}\right) = \theta_{i}\cdot x\\

log(p_{i}) = \theta_{i} \cdot x - log(z)\\
p_{i}= e^{\theta_{i} ~ \cdot ~ x ~-~ log(z)} = \dfrac{ e^{\theta_{i} ~ \cdot ~ x}}{z}\\\\

\text{Но ещё } ~~ \sum\limits p_{i}= 1 \leftrightarrow \dfrac{ \sum\limits e^{\theta_{i} ~ \cdot ~ x}}{z} = 1 \ \leftrightarrow z = \sum\limits e^{\theta_{i} ~ \cdot ~ x} \\\\ 

\text{Тогда }~~ p_{i}= \dfrac{e^{\theta_{i} ~ \cdot ~ x}}{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}} ~~ \text{ - эта функция называется softmax}\\

p =softmax(\Theta \cdot x) ~~\text{здесь $\theta $ - это матрица, а $p$ - это вектор}

}$$
Вариант, когда у нас есть отдельный k-тый класс?
$$\displaylines{
\tilde{\theta_{i}}= \theta_{i}- \theta_{k}, ~~~~\theta_{k}= 0\\

p_{i}= \dfrac{e^{\tilde{\theta_i}}}{1+\sum\limits_{j=1}^{k-1}e^{\tilde{\theta_{j}}~\cdot ~ x}} \\

p_{k} = \dfrac{1}{1+\sum\limits_{j=1}^{k-1}e^{\tilde{\theta_{j}}~\cdot ~ x}} \\\\

\text{Можно не использовать softmax:} ~~ p_{i}= \theta_{i}\cdot x\\

\text{Потому что softmax не меняет позицию максимума, он делает так, чтобы сумма }\\ \text{всех значений была равна единице. Но если мы хотим именно вероятности, } \\ \text{то нужно использовать softmax.} \\\\

\text{Если сначала посчитать все  $p_{i}$ без softmax, а только потом нормировать, то это будут} \\ \text{не вероятности, а что-то похожее на них. Отличие в том, что мы возводили  } \\ \text{ в экспоненту и это примерно тоже самое, как посчитать отношение элемента к сумме } \\ \text{ и отношение квадрата элемента к сумме квадратов элементов}
}$$ 
#### Считаем производную

$$\displaylines{L = -\sum\limits y_{i}~log ~ p_{i} ~~~\text{- функция для которой будем считать производную}\\\\

p_{i}=\dfrac{e^{\theta_{i} ~ \cdot ~ x}}{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}}\\



}$$

Мы хотим посчитать производную по всему: $\theta_{i}$ - вектор, $\theta_{j}$ - вектор. Хотим посчитать по каждому вектору и по каждому элементу вектора.

Нашей производной будет матрица

$$\displaylines{

\Theta = \left(\begin{matrix} \dfrac{\partial L}{\partial \theta_{11}  } \dfrac{\partial L}{\partial \theta_{12}}   \dots  \\\dfrac{\partial L}{\partial \theta_{21}  } \dfrac{\partial L}{\partial \theta_{22}} \dots \\ \dots 
 \end{matrix}\right) = \left\{\dfrac{\partial L}{\partial \theta_{i~l}}\right\}_{i=1,~ l = 1} ^{k,n} ~-~\text{матрица Грамма}
}$$

Проблема: в производной $p_i$ есть и $\theta_{i}$ и $\theta_j$.

Посчитаем прозводную L по $\theta$:
$$\displaylines{
L_{\theta}^{'} = y_{i} \cdot \dfrac{1}{p_{i}}\cdot (p_i)_{\theta}^{'} = y_{i}\cdot \dfrac{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}}{e^{\theta_{i} ~ \cdot ~ x}}\cdot (p_i)_{\theta}^{'} \tag{5} 
}$$

$$\displaylines{
\text{Посчитаем производную для диагональных элементов матрицы: когда для $p_i$ считаем  $\theta_i$}\\ \text{производную по $\theta_i$}\\

(p_i)_{\theta_i}^{'} = \left(\dfrac{e^{\theta_{i} ~ \cdot ~ x}}{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}}\right)_{\theta_i}^{'} 

= \dfrac{    (e^{\theta_{i}\cdot x}) _{\theta_{i}}^{'} ~ \cdot ~ \sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}   -      e^{\theta_{i}\cdot x}  ~\cdot~ \left(\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}\right)_{\theta_{i}}^{'} }

{\left(\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}\right)^{2}} =\\\\



}$$
$$\displaylines{

\text{Но от всей суммы нам понадобится только $e^{\theta_{i} ~ \cdot ~ x}$, потому что все остальные}\\ \text{ слагаемые стали равны нулю.}\\

=  \dfrac{    (e^{\theta_{i}\cdot x}) _{\theta_{i}}^{'} ~ \cdot ~ \sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}   -      e^{\theta_{i}\cdot x}  ~\cdot~ \left(e^{\theta_{i}\cdot x}\right)_{\theta_{i}}^{'} }

{\left(\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}\right)^{2}} =\\\\

=  \dfrac{    \left(e^{\theta_{i}\cdot x}  \cdot(\theta_{i}\cdot x)_{\theta_{i}}^{'} \right) ~ \cdot ~ \sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}   -      e^{\theta_{i}\cdot x}  ~\cdot~ \left(e^{\theta_{i}\cdot x}  \cdot(\theta_{i}\cdot x)_{\theta_{i}}^{'} \right) }

{\left(\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}\right)^{2}} =\\\\

=  \dfrac{    \left(e^{\theta_{i}\cdot x}  \cdot x \right) ~ \cdot ~ \sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}   -      e^{\theta_{i}\cdot x}  ~\cdot~ \left(e^{\theta_{i}\cdot x}  \cdot x \right) }

{\left(\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}\right)^{2}} =\\\\


=\left(e^{\theta_{i}\cdot x}  \cdot x \right) \cdot \dfrac{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x} - e^{\theta_{i}\cdot x} }

{\left(\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}\right)^{2}}



}$$

Итого 
$$\displaylines{(p_i)_{\theta_i}^{'} = 

\left(\dfrac{e^{\theta_{i} ~ \cdot ~ x}}{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}}\right)_{\theta_{i}}^{'} = 

\left(e^{\theta_{i}\cdot x}  \cdot x \right) \cdot \dfrac{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x} - e^{\theta_{i}\cdot x} }

{\left(\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}\right)^{2}}




}$$

А теперь вернёмся к формуле $(5)$ производной $L_{\theta_i}^{'}$ и подставим туда получившееся значение
$$\displaylines{
(L_i)_{\theta_i}^{'} = y_{i} \cdot \dfrac{1}{p_{i}}\cdot (p_i)_{\theta}^{'} =

y_{i}\cdot \dfrac{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}}{e^{\theta_{i} ~ \cdot ~ x}}\cdot (p_i)_{\theta}^{'}  =  

\left( y_{i}\cdot \dfrac{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}}{e^{\theta_{i} ~ \cdot ~ x}} \right)\cdot \left(\left(e^{\theta_{i}\cdot x}  \cdot x \right) \cdot \dfrac{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x} - e^{\theta_{i}\cdot x} }

{\left(\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}\right)^{2}} \right) = \\

=y_{i}\cdot x ~\cdot~ \dfrac{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x} - e^{\theta_{i}\cdot x} }{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}} = 

y_{i}\cdot x ~\cdot~ \left(\dfrac{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x} }{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}} - \dfrac{ - e^{\theta_{i}\cdot x}}{\sum\limits_{j=1}^{k} e^{\theta_{j} ~ \cdot ~ x}}\right) = y_{i}\cdot x \cdot (1-p_i)


}$$
Теперь посчитаем производную от этой же функции, но по другому $\theta$, теперь $j\neq i$ 
$$\displaylines{
(L_i)_{\theta_{j}}^{'} = \left(y_{i}~log ~ p_{i}\right)^{'}_{\theta_{j} } = y_{i} \cdot \dfrac{1}{p_{i}}\cdot (p_i)_{\theta_{j}}^{'}

= y_{i}\cdot \dfrac{\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}}{e^{\theta_{i} ~ \cdot ~ x}}~\cdot~ (p_i)_{\theta_{j}}^{'} \\
\\i \neq j\\ \tag{6}





}$$

$$\displaylines{(p_i)_{\theta_{j}}^{'} = \left(\dfrac{e^{\theta_{i} ~ \cdot ~ x}}{\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}}\right)_{\theta_{j}}^{'} 

= \dfrac{    (e^{\theta_{i}\cdot x}) _{\theta_{j}}^{'} ~ \cdot ~ \sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}   -      e^{\theta_{i}\cdot x}  ~\cdot~ \left(\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}\right)_{\theta_{j}}^{'} }

{\left(\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}\right)^{2}} =\\\\

\text{производная от числителя равна нулю, потому что он от $\theta_j$ не зависит:}\\

=\dfrac{-      e^{\theta_{i}\cdot x}  ~\cdot~ \left(\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}\right)_{\theta_{j}}^{'}}{\left(\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}\right)^{2}} = \\\\

\text{А среди всех $e^{\theta_{t}~\cdot ~x}$ числителя, только $e^{\theta_{j}~\cdot ~x}$ зависит от $\theta_j$ и только эта производная не стане нулём   }\\

=\dfrac{-      e^{\theta_{i}\cdot x}  ~\cdot~ \left( e^{\theta_{j} ~ \cdot ~ x}\right)_{\theta_{j}}^{'}}{\left(\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}\right)^{2}} 

=\dfrac{-      e^{\theta_{i}\cdot x}  ~\cdot~ \left( e^{\theta_{j} ~ \cdot ~ x}\right)~\cdot~x}{\left(\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}\right)^{2}}}$$

$$\displaylines{\text{А теперь вернёмся к формуле }(6) \text{и подставим } (p_i)_{\theta_{j}}^{'}: \\

(L_i)_{\theta_{j}}^{'} =  y_{i}\cdot \dfrac{\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}}{e^{\theta_{i} ~ \cdot ~ x}}~\cdot~ (p_i)_{\theta_{j}}^{'} =  

y_{i}\cdot \dfrac{\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}}{e^{\theta_{i} ~ \cdot ~ x}}~\cdot \dfrac{-      e^{\theta_{i}\cdot x}  ~\cdot~ \left( e^{\theta_{j} ~ \cdot ~ x}\right)~\cdot~x}{\left(\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}\right)^{2}} = \\\\

=-y_{i}\cdot x \dfrac{ e^{\theta_{j}\cdot x}}{\left(\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}\right)} = -y_{i}\cdot x \cdot p_j

}$$

Итого 
$$\displaylines{

L_{i,~\theta_{i}} = y_{i}\cdot x \cdot (1-p_i) \\

L_{i,~\theta_{j}} = -y_{i}\cdot x \cdot p_j 
}$$
#### Сигмоида
Сигмоида - функция, которая похожа на букву S. Мы используем логистическую сигмоиду


![[Pasted image 20250428153757.png]]
![[Pasted image 20250428153842.png]]
![[Pasted image 20250205220635.png]]

![[Pasted image 20250205220658.png]]


##### Дополнение 1
-------------
В **пункте 2** мы получили вероятность, например **0.8** (значит, модель считает, что объект с вероятностью 80% относится к классу 1).

Теперь **3. Обучение модели (MLE)** — это процесс подбора таких коэффициентов $β$, при которых вероятность **правильных предсказаний** будет **максимальной**.

![[Pasted image 20250205223203.png]]

##### Дополнение 2

![[Pasted image 20250205224616.png]]

##### **3. Почему мы ищем $β$**

![[Pasted image 20250205225737.png]]

$β$ — это **параметры модели**, которые мы **не знаем заранее**.

- Нам нужно найти такие $β$, при которых вероятность $L(β)$ **максимальна**.
- Для этого **берем логарифм**, затем **берем производные по $β$, а потом **решаем уравнение**.

👉 То есть **твоя функция правдоподобия на самом деле зависит от $β$, просто в твоем примере их уже подставили и посчитали.

##### Алгоритм кратко

![[Pasted image 20250205225835.png]]

##### 5. Пункт - два решения (Аналитическое или Градиент по классике)

![[Pasted image 20250205230002.png]]

----------
#### Вариант для меток -1, 1

![[Pasted image 20250205231114.png]]

![[Pasted image 20250205231225.png]]

![[Pasted image 20250205231238.png]]

### Функция ошибок

![[Pasted image 20250205232624.png]]

![[Pasted image 20250205232642.png]]

![[Pasted image 20250205232711.png]]

![[Pasted image 20250205232736.png]]

![[Pasted image 20250205232759.png]]
#### **Итог**

- **LogLoss** измеряет, насколько хорошо модель предсказывает вероятность для каждого класса, учитывая уверенность модели.

- Низкое значение LogLoss — это **хорошо**, потому что это значит, что модель правильно и уверенно предсказывает классы.

- Высокое значение LogLoss говорит о том, что модель делает **серьезные ошибки** (например, предсказания с высокой уверенностью для неправильных классов).

### Кросс-энтропия 

#### 2.1 Бинарная_кросс_энтропия

#Бинарная_кросс_энтропия

![[Pasted image 20241105003842.png]]

![[Pasted image 20241105003940.png]]

 Где лучше использовать:

- Бинарная кросс-энтропия используется в задачах **бинарной классификации**, когда необходимо предсказать одну из двух категорий (например, "да" или "нет").

- Применяется в задачах, таких как диагностика заболеваний (наличие/отсутствие болезни), спам-фильтрация (спам/не спам).
--------------------------

# Конспект с 2025 годом

## Вторая лекция (07.11.2025)

?? Посчитаем производную $softmax(\theta_{x})^{'}_{\theta}$ по i-тому классу 

$$\displaylines{

log(softmax(\theta\cdot{x})_{i})^{'}_{\theta_{i}} = \\

\text{считаем от softmax i-того, потому что берем от класса i,} \\
\text{ для которого ответ правильный}\\


log\left(\dfrac{e^{\theta_{i}\cdot x}}{\sum\limits e^{\theta_{j}\cdot x}} \right) = 

log(e^{\theta_{i}~\cdot~ x}) - log \sum\limits e^{\theta_{j}~\cdot~ x} = \\

=\theta_{i}~\cdot~ x - log\left(\sum\limits e^{\theta_{i}~\cdot~ x}\right)' = \\

=\dfrac{x ~\cdot~ e^{\theta_{i} ~\cdot~ x}}{\sum\limits_{j}e^{\theta_j~\cdot~x}}

}$$

Это если

#### Теперь посчитаем по k-тому элементу 
$k\neq j$


### Случай, когда не нулевая вероятность есть у каждого класса (часть 1)


$$\displaylines{ 
softmax_{n}=\{\hat{p}_{0,n} ~~ \hat{p}_{1,n}~~ \hat{p}_{2,n} ~~ \dots\}\\
\theta = \{\theta_{1}~,~\theta_{2}~,~\theta_{3}~~\dots\}\\
p=\{p_{0,n} ~~ p_{1,n}~~ p_{2,n} ~~ \dots\}\\
}$$

$$\displaylines{ 
\\ \\ \\\
\text{А теперь вернёмся к формуле }(6) \text{и подставим } (p_i)_{\theta_{j}}^{'}: \\

(L_i)_{\theta_{j}}^{'} =  y_{i}\cdot \dfrac{\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}}{e^{\theta_{i} ~ \cdot ~ x}}~\cdot~ (p_i)_{\theta_{j}}^{'} =  

y_{i}\cdot \dfrac{\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}}{e^{\theta_{i} ~ \cdot ~ x}}~\cdot \dfrac{-      e^{\theta_{i}\cdot x}  ~\cdot~ \left( e^{\theta_{j} ~ \cdot ~ x}\right)~\cdot~x}{\left(\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}\right)^{2}} = \\\\

=-y_{i}\cdot x \dfrac{ e^{\theta_{j}\cdot x}}{\left(\sum\limits_{t=1}^{k} e^{\theta_{t} ~ \cdot ~ x}\right)} = -y_{i}\cdot x \cdot p_j

}$$
