#Регуляризация #Переобучение #Функция_потерь #LASSO #Ridge #Elastic 

Сначала следует прочитать [[Понятие нормы]], [[Нормализация]], [[Функция потерь]], [[Разряженность]], [[Мультиколлинеарность]], [[Оценка переобучения]]

**Определение**: Регуляризация — это метод, используемый для предотвращения переобучения модели. Она добавляет штраф к функции потерь, который ограничивает сложность модели, тем самым улучшая обобщающую способность.

**Как работает**:

- В регуляризации часто используется норма (например, L1 или L2) для penalizing (наказания) больших значений коэффициентов модели.
- 
- Примеры:
    - **Lasso (L1-регуляризация)**: добавляет штраф на сумму абсолютных значений коэффициентов, что может приводить к обнулению некоторых из них и, как следствие, к отбору признаков.
    -  $$l_{1}= {\sum_{i = 1}^n\limits{|x_i|}}$$
    - **Ridge (L2-регуляризация)**: добавляет штраф на сумму квадратов коэффициентов, что помогает смягчить влияние высоких значений, но не приводит к обнулению коэффициентов.
     $$l_{2}= \sqrt{\sum_{i = 1}^n\limits{|x_i|^2}}$$

### Основные пункты

1. **Цель**:
    
    - **Регуляризация**: используется для улучшения обобщающей способности модели и предотвращения переобучения.
2. **Контекст использования**:
    
    - **Регуляризация**: обычно применяется в контексте машинного обучения и статистики для оптимизации моделей.

3. **Связь**:
    
    - Нормы используются в регуляризации для определения штрафов на значения коэффициентов модели, но регуляризация — это более широкая концепция, не сводящаяся только к нормам.

----------------------------

#### L1-регуляризация (Lasso)

- **Описание**:
    
    - Добавляет к функции потерь сумму абсолютных значений коэффициентов. Это приводит к тому, что некоторые коэффициенты могут стать равными нулю, что эффективно ведет к отбору признаков.

![[Pasted image 20241105005716.png]]

или (одно и тоже)

![[Pasted image 20241105005233.png]]

![[Pasted image 20241105010000.png]]

![[Pasted image 20241105010019.png]]

Параметр 𝜆  в Lasso-регрессии — это коэффициент регуляризации, который контролирует степень влияния L1-штрафа на модель. Он помогает сбалансировать между подгонкой модели к данным и её сложностью. Чем больше значение 𝜆, тем сильнее регуляризация, и, как следствие, больше вероятность, что некоторые коэффициенты станут равными нулю.
##### Ещё одно применение
Можно понять какие признаки более-менее бесполезны -> их можно выкинуть -> понизить размерность.

##### Как выбирать λ

4. **Кросс-валидация**:
    
    - Один из самых распространённых способов выбрать λ — это использовать кросс-валидацию. В этом методе данные делятся на несколько частей (folds), и модель обучается на одной части данных, а затем тестируется на оставшихся. Процесс повторяется для разных значений λ, и выбирается то значение, которое даёт наилучшие результаты на валидационном наборе.

    - ![[Pasted image 20241105012806.png]]
    
    - Формально, при использовании k-слойной кросс-валидации процесс выглядит следующим образом: (вместо MSE, могут быть другие функции так что там стоит написать)
	- ![[Pasted image 20241105012350.png]]
	- **Согласованность:** Использование той же функции потерь в CV Error помогает обеспечить согласованность между тем, как вы обучаете модель и как оцениваете её производительность.
	
	- **Регуляризация:** Если вы добавляете регуляризацию, её учитывают только в процессе обучения модели, а для кросс-валидации вы можете использовать оригинальную функцию потерь, чтобы оценить качество модели.

	  Так что, да, можно использовать ту же функцию, что и в Loss, для CV Error!
    
5. **Поиск по сетке (Grid Search)**:
    
    - В этом методе создаётся набор потенциальных значений λ, и модель обучается для каждого из них с использованием кросс-валидации. После этого выбирается значение λ с наименьшей ошибкой на валидационном наборе.
    
6. **Адаптивные методы**:
    
    - Некоторые алгоритмы могут использовать адаптивные подходы к выбору λ, корректируя его во время обучения, в зависимости от результатов.
    
7.  **Интуитивные подходы**:
    
    - Если у вас есть понимание данных и модели, вы можете начать с небольшого значения λ и постепенно увеличивать его, наблюдая за изменениями в качестве модели.

###### Рекомендации

- Часто значения λ выбираются в диапазоне от 0 до 1, но в зависимости от задачи и данных, они могут принимать и более высокие значения.

- Важно помнить, что слишком большое значение λ может привести к недообучению модели, так как она может стать слишком простой и не улавливать зависимости в данных.

#### L2-регуляризация (Ridge)

L2-регуляризация (также известная как регуляризация Тихонова или Ridge-регуляризация) добавляет к функции потерь штраф за величину весов модели, что помогает предотвратить переобучение. Этот штраф равен сумме квадратов весов, умноженной на коэффициент регуляризации.

![[Pasted image 20241107003326.png]]

Чем больше значение λ, тем сильнее L2-регуляризация подавляет большие значения весов, что помогает модели обобщать данные и снижает вероятность переобучения.

Функция потерь MSE с L2

![[Pasted image 20241107003609.png]]

![[Pasted image 20241107004701.png]]

![[Pasted image 20241107004724.png]]

![[Pasted image 20241107004744.png]]

Редкий случай с индивидуальными весами (L1 и L2) (РЕДКИЕ ПРИМЕРЫ)

![[Pasted image 20241107005102.png]]

![[Pasted image 20241107005148.png]]

Ещё иногда могут делать вот так

![[Pasted image 20241107005302.png]]

#### Регуляризация (Elastic Net)

**Elastic Net** — это регуляризация, которая комбинирует оба типа регуляризации: **L1 (Lasso)** и **L2 (Ridge)**. Это полезно, когда ваши данные имеют высокую корреляцию между признаками, и вы хотите использовать преимущества обеих регуляризаций. Elastic Net сохраняет преимущества L1 (отбор признаков) и L2 (устранение переобучения), что делает его более гибким.

![[Pasted image 20241107005530.png]]

### Что делает Elastic Net?

- **L1-часть (Lasso)** — штрафует абсолютные значения весов, что приводит к занулению некоторых весов и, как следствие, к отбору признаков.
- **L2-часть (Ridge)** — штрафует квадраты весов, что помогает избежать переобучения и делает модель более устойчивой к шуму, особенно при наличии многоколлинеарности между признаками.

![[Pasted image 20241107005648.png]]

- **Хорошо работает с коррелированными признаками**. Если признаки сильно коррелированы, **Lasso** может выбрать один признак и отбросить другие, в то время как **Elastic Net** может сохранить все коррелированные признаки.
    
- **Гибкость** в выборе того, какой вес (L1 или L2) использовать, а также позволяет использовать комбинацию обоих.

![[Pasted image 20241107005707.png]]

 Интерпретация

- Если λ1 слишком велико, модель будет склонна использовать только те признаки, которые имеют значимые веса, и будет занулять остальные (аналог Lasso).
- Если λ2 слишком велико, модель будет склонна использовать более устойчивые весы, минимизируя их квадраты (аналог Ridge).
- Использование обоих коэффициентов позволяет моделям Elastic Net быть устойчивыми к переобучению, но в то же время оставлять возможности для отбора признаков.

![[Pasted image 20241107005800.png]]

Таким образом, Elastic Net с различными весами для каждого примера будет сочетать L1 и L2-регуляризацию, помогая отбирать важные признаки, одновременно сохраняя стабильность модели.


#### Где что использовать?

Выбор между L1, L2 и Elastic Net зависит от данных, задачи, требований к интерпретации модели и цели применения. Давайте разберем, когда и какую регуляризацию лучше использовать:

##### 1. L1-регуляризация (Lasso)

Используется, когда:

- **Нужен отбор признаков**: L1-регуляризация зануляет некоторые веса, что автоматически исключает признаки, связанные с этими весами. Это помогает выбрать только важные признаки.
- **Есть много признаков, но лишь немногие из них важны**. L1 работает хорошо, если предположить, что большинство признаков не связаны с целевой переменной.
- **Признаки не сильно коррелированы**: L1 может случайным образом выбирать один из коррелированных признаков и занулять остальные, что может привести к нестабильности, если у вас есть группы сильно коррелированных признаков.

Пример применения:

- **Отбор признаков** в задачах, где используется большое количество признаков (например, в задачах с текстом или при анализе изображений).
- **Интерпретируемость**: L1 подходит для моделей, где важна интерпретируемость, так как обнуленные веса облегчают объяснение результатов.

##### 2. L2-регуляризация (Ridge)

Используется, когда:

- **Признаки коррелированы**: L2 лучше подходит для ситуаций с высокой корреляцией между признаками, так как она сглаживает веса, вместо их обнуления, и снижает влияние переобучения за счет регулирования величины весов.
- **Нужна более стабильная модель**: L2-регуляризация приводит к более устойчивым решениям, которые меняются плавно при добавлении небольшого количества шума в данные.
- **Нужна точная предсказательная мощность, а не отбор признаков**: L2 делает модель более устойчивой к переобучению, но не помогает с явным отбором признаков.

Пример применения:

- **Прогнозирование** в задачах, где большинство признаков полезны и не должны быть полностью исключены.
- **Модели с коррелированными признаками**: L2 отлично работает для моделей с большим количеством взаимосвязанных признаков (например, задачи прогнозирования в экономике или медицине).

##### 3. Elastic Net (комбинация L1 и L2)

Используется, когда:

- **Нужен компромисс между L1 и L2**: Elastic Net сочетает отбор признаков (L1) и устойчивость к коррелированным признакам (L2), что делает его универсальным решением.
- **Есть сильно коррелированные признаки, и нужен отбор признаков**: Elastic Net может сохранить все коррелированные признаки с ненулевыми весами, а также занулить менее значимые признаки, которые не коррелированы с целевой переменной.
- **Существует много признаков, среди которых нужно сделать выбор**: Elastic Net помогает найти баланс, который позволяет отобрать важные признаки, одновременно избегая нестабильности из-за коррелированных признаков.

Пример применения:

- **Геномика** и **анализ данных высокой размерности**: в этих задачах, где есть много коррелированных и малозначимых признаков, Elastic Net помогает выделить важные признаки.
- **Финансовые модели**: когда много факторов могут быть коррелированы, и требуется отбор признаков для стабильной и предсказательной модели.

##### Резюме выбора

8. **L1-регуляризация** (Lasso) — для **разреженных моделей**, где нужен отбор признаков и признаки не сильно коррелированы.
9. **L2-регуляризация** (Ridge) — для **устойчивых моделей**, где признаки коррелированы и важна устойчивость, а не отбор признаков.
10. **Elastic Net** — для **баланса между отбором признаков и устойчивостью**. Подходит для ситуаций, где признаки сил

![[Pasted image 20250205171443.png]]

![[Pasted image 20250205171458.png]]

